# e04s02-tasks.yaml
story_id: e04s02
title: "Backfill foreground + cursor + deduplicación"
status: passing
bcps: 10
risk: P0
context: domain
tasks:
  - id: 1
    description: >-
      services/backfill.py: lógica de cursor (§10) — comparación estrictamente < (nunca ==);
      scope_cursor (snapshot para break) separado del cursor móvil (L-F1); upload_date ausente →
      fallback al cursor anterior ACTUALIZADO (L-F2); el cursor SOLO avanza en estado terminal
      (downloaded/failed/skipped — NO cancelled).
    verify: "uv run pytest tests/backfill/test_backfill_fg.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "backfill"
        - "cursor"
  - id: 2
    description: >-
      services/backfill.py: run_backfill() foreground — consulta cuentas, lista feed dentro del try
      catástrofe (F-10); usa el motor (e04s01) con cookies (F-01: get_working_cookie, aborta
      backfill.no_cookies si ninguna); descarga con --download-archive; contabilidad F-09
      (backfill_total al iniciar, backfill_done acumulativo incl. skipped); al completar transición
      consumible --then-monitor (§10).
    verify: "uv run pytest tests/backfill/test_backfill_fg.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "backfill"
        - "runner"
  - id: 3
    description: >-
      services/backfill.py: cancelación cooperativa (T21) — relectura periódica del estado;
      persistencia del cursor con UPDATE condicional WHERE backfill_status='backfilling' (rowcount 0
      = cancelación detectada, L-F5/L-F6); retorno temprano del outcome 'cancelled'. except
      asyncio.CancelledError (BaseException) → vuelve a 'queued' (F-10). reconcile_stale_backfills()
      para el arranque (F-10).
    verify: "uv run pytest tests/backfill/test_backfill_fg.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "backfill"
        - "cancel"
  - id: 4
    description: >-
      cli/backfill.py: backfill run (foreground, barra de progreso rich), status. Servicios:
      handle_download_result parcial (estados, expected_has_video T55, error_category). Descarte del
      archive antes del reintento (T24, integración con e04s01).
    verify: "uv run pytest tests/backfill/test_backfill_cli.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "backfill"
        - "cli"
  - id: 5
    description: >-
      Tests (F.I.R.S.T., §14): test_backfill_fg.py — cursor < (borde exacto), scope vs móvil (L-F1),
      upload_date ausente (L-F2), F-09 (total/done incl. skipped), F-10 (CancelledError → queued,
      reconcile), T21/L-F5/L-F6 (rowcount 0 → cancelled), F-01 (no_cookies). test_backfill_cli.py —
      run/status. SQLite :memory: + StaticPool; mocks de motor; nunca entorno real (T69).
    verify: "uv run pytest tests/backfill/ -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "backfill"
        - "unit"
