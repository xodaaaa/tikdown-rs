# e04s01-tasks.yaml
story_id: e04s01
title: "DownloadEngine (formato, rotación, cooldown global)"
status: todo
bcps: 13
tasks:
  - id: e04s01-t01
    description: >-
      services/downloader.DownloadEngine (§4): yt-dlp SIEMPRE vía asyncio.to_thread con timeout 10
      min/vídeo; formato por defecto bestvideo[height<=1080][ext=mp4]+bestaudio[ext=m4a]/... con
      override global DOWNLOAD_FORMAT (§4.2); merge_output_format mp4; formato mejorado para reintento
      ante solo-audio; params impersonate = objetos ImpersonateTarget rotados (L-D1); rotación
      round-robin de cookies válidas + targets; nunca httpx/requests a TikTok (§4.2).
    verify: "uv run pytest tests/downloader/test_engine.py -q"
    status: todo
  - id: e04s01-t02
    description: >-
      Cooldown global entre procesos vía SQLite (download_pacing_state, T22): CAS atómico con
      next_allowed_at en ms (L-C7); consultado junto a network_available y downloads_paused antes de
      cada intento (T45). DownloadEngine consulta las tres antes de cada descarga.
    verify: "uv run pytest tests/downloader/test_pacing.py -q"
    status: todo
  - id: e04s01-t03
    description: >-
      Clasificación de fallos (§0.11, §4): definitivo (auth inválida, suspensión, captcha,
      inexistente), transitorio (rate-limit/anti-bot, timeouts, degradación extractor), integrity.
      error_category persistido en videos; limpieza post-éxito best-effort (T14).
    verify: "uv run pytest tests/downloader/test_failure_classification.py -q"
    status: todo
