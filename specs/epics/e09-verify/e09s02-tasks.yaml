# e09s02-tasks.yaml
story_id: e09s02
title: "videos export (json/csv) + system backup/disk"
status: todo
bcps: 8
tasks:
  - id: e09s02-t01
    description: >-
      services/export: export de metadatos json/csv; CSV con csv.writer stdlib + quoting RFC 4180 +
      sanitización de prefijos = + - @ y espacios/tabs iniciales contra inyección de fórmulas
      (T49/F-11). videos export sale sin wrap ni markup de Rich (L-A6).
    verify: "uv run pytest tests/export/test_export.py -q"
    status: todo
  - id: e09s02-t02
    description: >-
      services/backup: snapshot consistente en caliente de la DB vía API de backup / VACUUM INTO en
      DATA_DIR/backups/ (NUNCA copiar .db a pelo bajo WAL, F-21b); mapea sqlite3.OperationalError a
      error limpio; purga snapshots por encima de SYSTEM_BACKUP_RETAIN_COUNT. system disk con --resume
      (disco lleno, T45) y system backup.
    verify: "uv run pytest tests/backup/test_backup.py -q"
    status: todo
