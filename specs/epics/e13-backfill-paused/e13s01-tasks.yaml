# e13s01-tasks.yaml
story_id: e13s01
title: "Estado 'paused' real + recogida automática + slot cross-proceso (T22)"
status: failing
bcps: 6
risk: P1
context: domain
tasks:
  - id: 1
    description: >-
      services/backfill.py: productor de 'paused' — en run_backfill, el except asyncio.CancelledError
      distingue la causa: si downloads_paused (disco) o red offline → backfill_status='paused'
      (nuevo estado real, §2 reservado sin productor); si no → 'queued' (F-10 crash). El estado
      'paused' NO es terminal para el cursor (igual que 'queued'). Marcar también pause_reason
      (campo nuevo) para diagnóstico.
    verify: "uv run pytest tests/backfill/test_backfill_paused.py -q"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "backfill"
        - "state"
  - id: 2
    description: >-
      models/models.py: nueva tabla singleton backfill_slot (patrón T22 — id=1, CHECK(id=1),
      INSERT ... ON CONFLICT DO NOTHING con commit inmediato L-C6). Campos: id, owner TEXT NULL,
      acquired_at TEXT. El slot es CROSS-PROCESO real: adquisición atómica con
      UPDATE backfill_slot SET owner=:me, acquired_at=:now WHERE id=1 AND owner IS NULL
      RETURNING owner (CAS vía SQLite, igual que download_pacing_state T22). Liberación con
      UPDATE ... SET owner=NULL WHERE id=1 AND owner=:me. Migración Alembic para la tabla.
    verify: "uv run pytest tests/backfill/test_backfill_paused.py -q"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "backfill"
        - "slot"
  - id: 3
    description: >-
      services/backfill.py: reemplazar el asyncio.Lock por proceso por el slot cross-proceso
      (backfill_slot). backfill_slot_busy()/acquire_slot()/release_slot() usan la tabla SQLite
      (CAS atómico) en vez del lock en memoria. El slot es visible para todos los procesos
      (daemon + CLI foreground + bot) — un backfill de CLI y uno del daemon NO se solapan (T22).
    verify: "uv run pytest tests/backfill/test_backfill_paused.py -q"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "backfill"
        - "slot"
  - id: 4
    description: >-
      services/backfill.py: collect_queued_backfills() recoge 'queued' Y 'paused' cuando la pausa
      se resuelve (F-10): condición = downloads_paused falso Y red online. Para 'paused' con
      pause_reason resuelta → reanuda a 'backfilling' (igual que queued). El job del daemon llama
      a esto periódicamente (registrar job en run.py _register_jobs, max_instances=1, T44).
    verify: "uv run pytest tests/backfill/test_backfill_paused.py -q"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "backfill"
        - "collect"
  - id: 5
    description: >-
      Tests (F.I.R.S.T., §14): test_backfill_paused.py — (a) cancel con disco paused → 'paused';
      (b) cancel sin causa → 'queued'; (c) slot cross-proceso: adquisición atómica (dos procesos
      simulados, solo uno gana); (d) collect incluye paused reanudables y excluye los que siguen
      con pausa; (e) reconcile_stale_backfills no toca 'paused'. SQLite :memory: + StaticPool (T69).
    verify: "uv run pytest tests/backfill/ -q"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "backfill"
        - "unit"
