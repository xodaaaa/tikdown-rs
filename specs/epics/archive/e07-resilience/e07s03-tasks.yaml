# e07s03-tasks.yaml
story_id: e07s03
title: "Circuit breaker por cuenta (auth -> paused + needs_review)"
status: passing
bcps: 6
risk: P0
context: domain
tasks:
  - id: 1
    description: >-
      core/breaker.py: AccountBreaker — contador en MEMORIA por cuenta (§4.4, se resetea al
      reiniciar); umbral 5 fallos de AUTH consecutivos → pausa la cuenta (paused + needs_review en
      DB, que sí persiste). record_result(username, category): auth → +1; exito → reset; transitorio
      → no cuenta.
    verify: "uv run pytest tests/circuit/test_breaker.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "breaker"
        - "auth"
  - id: 2
    description: >-
      core/breaker.py: solo fallos de AUTH cuentan (T52: 'requiring login', 'log into an account',
      'log in for access' via classify_failure). T5: 403 sin hints de auth → transitorio, NO cuenta.
      T45/T64: red/disco NO cuentan. Al disparar → paused + needs_review (helpers T37) + evento
      monitor.account_paused (F-08).
    verify: "uv run pytest tests/circuit/test_breaker.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "breaker"
        - "classification"
  - id: 3
    description: >-
      Tests (F.I.R.S.T., §14): test_breaker.py — 5 auth consecutivos → paused+needs_review; 4 auth +
      1 transitorio → NO pausa (transitorio no cuenta); exito resetea; red/disco no cuentan (T45/T64);
      evento monitor.account_paused emitido (F-08); contador en memoria (nueva instancia = reset).
      SQLite :memory: + StaticPool; nunca entorno real (T69).
    verify: "uv run pytest tests/circuit/ -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "breaker"
        - "unit"
