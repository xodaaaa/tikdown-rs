# e08s01-tasks.yaml
story_id: e08s01
title: "Estructura typer: 7 grupos + cli/common.py"
status: todo
bcps: 8
tasks:
  - id: e08s01-t01
    description: >-
      cli/main.py: app typer con @app.callback global --version e invoke_without_command=True
      (sin él, tikdown-rs --help lanza RuntimeError, L-A1); grupos registrados con
      app.add_typer(grupo, name="sustantivo") — exactamente 7: daemon, monitor, accounts, backfill,
      cookies, videos, system. Sin comandos-verbo sueltos en la raíz (§3). Pares simétricos
      pause/resume; borrado siempre remove.
    verify: "uv run tikdown-rs --help"
    status: todo
  - id: e08s01-t02
    description: >-
      cli/common.py (T18): wrapper central asyncio.run() para comandos síncronos; run_or_exit()
      convierte errores de negocio (AccountError, BackfillAccountError, ConfigurationError) en
      'ERROR <mensaje>' + exit 1 sin tracebacks (F-21); aplica migraciones idempotentes pendientes
      (§5.5) y construye Settings fresca por invocación (§5.6).
    verify: "uv run pytest tests/cli/test_common.py -q"
    status: todo
