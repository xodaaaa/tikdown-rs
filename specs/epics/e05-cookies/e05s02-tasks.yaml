# e05s02-tasks.yaml
story_id: e05s02
title: "Importación, listado, prueba y validación triestado"
status: passing
bcps: 9
risk: P0
context: domain
tasks:
  - id: 1
    description: >-
      services/cookies.py: add(path, keep_source=False) — importa cookies.txt/.json, cifra con
      Fernet, guarda en encrypted_blob. Borrado fuente best-effort (T14: fallo no rompe éxito);
      --keep-source conserva (F-15); informa destino real. Tempfile Netscape con header (T73).
      Clamp de expiración absurda a año 2100 (T33: min(timestamp, 2100)).
    verify: "uv run pytest tests/cookies/test_cookies.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "cookies"
        - "import"
  - id: 2
    description: >-
      services/cookies.py: validación triestado (§7) — validate_cookie() → valid/invalid/inconclusive.
      Solo auth CONFIRMADO → invalid; extractor/red/timeout → inconclusive. F-16: inconclusive NO
      toca validation_state ni last_validated_at (permite autocuración). 'no entries' → inconclusive.
      list (validation_state, motivo, countdown), test <id>, remove.
    verify: "uv run pytest tests/cookies/test_cookies.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "cookies"
        - "validation"
  - id: 3
    description: >-
      services/cookies.py: sonda robusta (T57/T74/L-E4/R12) — itera las primeras PROBE_MAX_ENTRIES=5
      entradas del feed buscando formatos de vídeo (L-E4); COOKIE_VALIDATION_URL acepta lista
      separada por comas con fallback en orden (R12); sonda rota (todas las candidatas fallan) →
      ciclo inconclusive GLOBAL sin tocar estados + cookie.validation_probe_failed (T57).
    verify: "uv run pytest tests/cookies/test_validation.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "cookies"
        - "probe"
  - id: 4
    description: >-
      services/cookies.py: get_working_cookie() (L-E3/§7/T32) — cookies valid ordenadas por
      last_validated_at desc; revalida activamente si el chequeo es antiguo; fallback a cookies
      anteriores si la primera falla; SOLO rechaza ante 'invalid' (L-E3: inconclusive conserva con
      log informativo). Sesiones cortas (T32: leer blob -> cerrar sesión -> validar -> reabrir para
      persistir).
    verify: "uv run pytest tests/cookies/test_validation.py -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "cookies"
        - "working"
  - id: 5
    description: >-
      cli/cookies.py: cookies add (--keep-source), list, test <id>, remove. Solo orquesta
      services/cookies (regla de oro §3). Tests (F.I.R.S.T., §14): test_cookies.py — import F-15/T14,
      triestado F-16, clamp T33; test_validation.py — sonda T57/T74/R12, L-E3, T32. Clave Fernet
      generada al vuelo (F-12); SQLite :memory: + StaticPool; mocks; nunca entorno real (T69).
    verify: "uv run pytest tests/cookies/ -q"
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "cookies"
        - "cli"
        - "unit"
