# e01s04-tasks.yaml
story_id: e01s04
title: "Modelos SQLAlchemy + migración Alembic + WAL"
status: todo
bcps: 8
tasks:
  - id: e01s04-t01
    description: >-
      Modelos SQLAlchemy async (§2): monitored_accounts (mode, paused, needs_review,
      notify_on_download, monitor_after_backfill, backfill_status/backfill_cursor/total/done,
      last_check_at, profile counts), videos (tiktok_video_id UNIQUE, account_id FK, url/title/
      description/duration, upload_date YYYYMMDD, local_path absoluto T8, file_size, file_hash,
      status downloaded|failed|cancelled|skipped, retry_count, error_message, error_category
      definitive|transient|integrity), cookies (encrypted_blob LargeBinary, validation_state
      valid|invalid|inconclusive, last_validated_at, last_validation_reason), daemon_state
      (singleton CHECK id=1), download_pacing_state (singleton, next_allowed_at ms), 
      download_archive, pending_notifications.
    verify: "uv run pytest tests/models/test_models.py -q"
    status: todo
  - id: e01s04-t02
    description: >-
      Migración Alembic inicial con todos los CHECK constraints — backfill_status incluye 'cancelled'
      desde el primer esquema (L-F7). PRAGMA journal_mode=WAL en engine. Creación idempotente de
      singletons con INSERT ... ON CONFLICT DO NOTHING + commit inmediato (T17, L-C6).
    verify: "uv run alembic upgrade head"
    status: todo
  - id: e01s04-t03
    description: "Tests de esquema: columnas, índices recomendados (§2), CHECK constraints, singleton idempotente bajo concurrencia."
    verify: "uv run pytest tests/models/test_schema.py -q"
    status: todo
