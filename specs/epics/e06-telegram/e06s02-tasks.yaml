# e06s02-tasks.yaml
story_id: e06s02
title: "Comandos de paridad funcional con CLI + notificaciones"
status: todo
bcps: 10
tasks:
  - id: e06s02-t01
    description: >-
      daemon/telegram/handlers.py: comandos planos /stats /disk /list /last /backfill /cookies etc.
      (paridad FUNCIONAL con CLI — misma función de services/* detrás, nunca duplicar lógica §3, §6.4).
      Salida de services/* reutilizada; sin markup rico en el mensaje (L-A6).
    verify: "uv run pytest tests/telegram/test_handlers.py -q"
    status: todo
  - id: e06s02-t02
    description: >-
      Notificaciones exhaustivas (§0.9): problemas de cookies, formato, backfills, red, disco.
      Envía vía services/notifications; reutiliza eventos del spool pending_notifications (§8, §9).
      Drena spool en transición a online y en arranque del daemon.
    verify: "uv run pytest tests/telegram/test_notifications.py -q"
    status: todo
