story_id: e02s04
verified_at: "2026-08-28T04:30:00Z"
verifier: verify-work
mode: smoke
phases:
  smoke:
    passed: true
    note: "Heartbeat persiste, contención ventana rotativa (§5.8), frescura (T50)"
  build:
    passed: true
    command: "uv build"
  lint:
    passed: true
    command: "uv run ruff check ."
  tests:
    passed: true
    count: 55
    command: "uv run pytest -q"
  terminal_verdict:
    command: "uv run pytest -q"
    exit_code: 0
    captured_at: "2026-08-28T04:29:00Z"
    note: "Single contiguous run — 55 passed"
  manual:
    steps:
      - step: "Heartbeat persiste (T37)"
        expected: "last_heartbeat_at + pid persistidos"
        actual: "OK: True, pid 4242"
        passed: true
      - step: "Contención ventana rotativa (§5.8)"
        expected: "record_busy x2 → busy_count 2"
        actual: "OK: 2"
        passed: true
      - step: "Frescura heartbeat (T50)"
        expected: "age <= 3x intervalo (30s)"
        actual: "OK: 0.04s <= 30s"
        passed: true
gaps:
  closed: true
