story_id: e09s01
verified_at: "2026-08-28T12:30:00Z"
verifier: verify-work
mode: smoke
phases:
  smoke:
    passed: true
    note: "Integridad: verify (size+SHA-256+ffprobe), T13 (--), T55 (slideshow)"
  build:
    passed: true
    command: "uv build"
  lint:
    passed: true
    command: "uv run ruff check ."
  tests:
    passed: true
    count: 181
    command: "uv run pytest -q"
  terminal_verdict:
    command: "uv run pytest -q"
    exit_code: 0
    captured_at: "2026-08-28T12:29:00Z"
    note: "181 passed"
  manual:
    steps:
      - step: "verify_video ok"
        expected: "ok True, sha 64 hex, size > 0"
        actual: "OK: True/64/4000"
        passed: true
      - step: "ffprobe con -- (T13)"
        expected: "'--' en args antes de la ruta"
        actual: "OK: True"
        passed: true
      - step: "T55 slideshow vs integrity"
        expected: "skipped/integrity/downloaded"
        actual: "OK"
        passed: true
gaps:
  closed: true
