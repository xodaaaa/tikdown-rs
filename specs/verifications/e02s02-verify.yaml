story_id: e02s02
verified_at: "2026-08-28T03:30:00Z"
verifier: verify-work
mode: smoke
phases:
  smoke:
    passed: true
    note: "Selfcheck impersonación real OK; ffmpeg detecta ausencia (T46 correcto); versión T4"
  build:
    passed: true
    command: "uv build"
  lint:
    passed: true
    command: "uv run ruff check ."
  tests:
    passed: true
    count: 42
    command: "uv run pytest -q"
  terminal_verdict:
    command: "uv run pytest -q"
    exit_code: 0
    captured_at: "2026-08-28T03:29:00Z"
    note: "Single contiguous run — 42 passed"
  manual:
    steps:
      - step: "Impersonación TLS real"
        expected: "selfcheck_impersonation() True"
        actual: "OK: True (37 targets)"
        passed: true
      - step: "ffmpeg/ffprobe (T46)"
        expected: "detecta ausencia con fail-fast"
        actual: "OK: SystemExit(1) — ffmpeg no instalado en este host (dependencia dura, se instala en Docker)"
        passed: true
      - step: "Versión yt-dlp interna (T4)"
        expected: "coincide con tag GitHub, no gestor"
        actual: "OK: 2026.08.27.231323"
        passed: true
gaps:
  closed: true
  note: "ffmpeg ausente es del entorno de dev (Windows); Docker lo instala (§11). El selfcheck T46 lo detecta correctamente."
