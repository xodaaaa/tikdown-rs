story_id: e02s01
verified_at: "2026-08-28T03:00:00Z"
verifier: verify-work
mode: smoke
phases:
  smoke:
    passed: true
    note: "Supervised tasks: crear, auditar excepción (T1), drenar (T28)"
  build:
    passed: true
    command: "uv build"
  lint:
    passed: true
    command: "uv run ruff check ."
  tests:
    passed: true
    count: 30
    command: "uv run pytest -q"
  terminal_verdict:
    command: "uv run pytest -q"
    exit_code: 0
    captured_at: "2026-08-28T02:59:00Z"
    note: "Single contiguous run — 30 passed"
  manual:
    steps:
      - step: "Tarea completa con valor"
        expected: "resultado 42"
        actual: "OK: True / 42"
        passed: true
      - step: "Excepción auditada (T1 callback síncrono)"
        expected: "RuntimeError capturado por el callback"
        actual: "OK: boom-uat"
        passed: true
      - step: "Drenaje cancela tarea larga (T28)"
        expected: "t3.cancelled() True"
        actual: "OK: True"
        passed: true
gaps:
  closed: true
