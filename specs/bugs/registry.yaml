# Bug registry - TikDown-rs

# Registro de bugs encontrados en la ronda de pruebas pseudo-produccion (rama `vibe`),
# rondas 1 y 2 (2026-08-28/29). Todos corregidos y mergeados via PR #1 y PR #2.
# Notas: el comando `videos` (feature e09), el refactor de impersonate targets al engine
# y los tests de formato NO son bugs - se excluyen del registry (features/refactors).

bugs:
  - bug_id: "BUG-2026-08-28T221000"
    date: "2026-08-28"
    severity: "critical"
    priority: "p0"
    scope: "daemon-cli"
    what_happened: "El contenedor Docker crash-lopeaba al arrancar: la CLI no tenia el subcomando `daemon run` que el Dockerfile usa como CMD y healthcheck."
    what_expected: "`tikdown-rs daemon run` debe arrancar el daemon (heartbeat, monitor, bot)."
    steps_to_reproduce:
      - "docker compose up -d"
      - "Observar logs: No such command 'run' (crash-loop)"
    root_cause: "El comando `run` no estaba registrado en el grupo CLI `daemon`; daemon/run.py::run_daemon existia pero era inalcanzable desde la CLI."
    files_changed: "src/tikdown_rs/cli/daemon.py"
    approach: "Registrar @app.command('run') que delega en daemon/run.py::run_daemon."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(cli): registrar comando daemon run (entrypoint del contenedor)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221100"
    date: "2026-08-28"
    severity: "critical"
    priority: "p0"
    scope: "docker"
    what_happened: "El daemon fallaba al aplicar migraciones con FileNotFoundError: No se encontro alembic.ini - el stage runtime de la imagen no tenia los recursos de migracion."
    what_expected: "La imagen debe incluir alembic.ini y alembic/ para que Alembic aplique migraciones en el arranque."
    steps_to_reproduce:
      - "docker compose build && docker compose up -d"
      - "Observar FileNotFoundError en logs (alembic.ini)"
    root_cause: "El Dockerfile solo copiaba src/ al builder; el runtime (que copia solo el .venv) no heredaba alembic.ini ni alembic/."
    files_changed: "Dockerfile"
    approach: "Copiar alembic.ini + alembic/ al stage runtime desde el builder."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix: copiar alembic/ al runtime"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221200"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "daemon"
    what_happened: "El job de heartbeat lanzaba RuntimeWarning: coroutine was never awaited y error cada 10s - APScheduler 3.11 ejecutaba el wrapper sync sin esperar la corrutina."
    what_expected: "El heartbeat debe ejecutarse correctamente cada intervalo sin warnings ni errores."
    steps_to_reproduce:
      - "docker compose up -d"
      - "docker compose logs: Job ..._schedule_heartbeat raised an exception (was never awaited)"
    root_cause: "Los wrappers de jobs eran def sincronos que creaban corrutinas sin await; APScheduler 3.11 no espera corrutinas en callbacks sync."
    files_changed: "src/tikdown_rs/daemon/run.py"
    approach: "Convertir los jobs en async def con await create_supervised_task(...)."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(daemon): jobs async de APScheduler - esperar tarea supervisada"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221300"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "backfill-engine"
    what_happened: "El backfill fallaba con AttributeError: 'YtDlpEngine' object has no attribute 'extract_profile' - el motor real no implementaba el metodo que el Protocol declaraba."
    what_expected: "extract_profile debe listar el feed de una cuenta para que el backfill pueda descargarlo."
    steps_to_reproduce:
      - "tikdown-rs backfill run <user>"
      - "Observar backfill.failed + AttributeError"
    root_cause: "YtDlpEngine solo implementaba download; extract_profile/list_videos/validate_cookie solo existian en el Protocol."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "Implementar extract_profile con yt-dlp extract_info(download=False)."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): implementar YtDlpEngine.extract_profile"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221400"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "backfill-cli"
    what_happened: "El comando backfill run pasaba cookies=[] hardcodeado, asi que run_backfill lanzaba NoCookiesError siempre (F-01) aunque hubiera cookies validas."
    what_expected: "El backfill debe usar las cookies working almacenadas (cifradas) para descargar con sesion."
    steps_to_reproduce:
      - "Importar cookies (cookies add)"
      - "tikdown-rs backfill run <user>"
      - "Observar ERROR backfill.no_cookies"
    root_cause: "La CLI no cargaba working_cookies_list(); el contenedor del backfill no recibia las cookies del servicio."
    files_changed: "src/tikdown_rs/cli/backfill.py"
    approach: "Cargar working_cookies_list() y pasarlas a run_backfill."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): cargar cookies working en backfill run (F-01)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221500"
    date: "2026-08-28"
    severity: "medium"
    priority: "p1"
    scope: "backfill-engine"
    what_happened: "extract_profile sin flat_playlist resolvia cada entrada del feed, y TikTok bloqueaba la resolucion completa - fallo intermitente del listado."
    what_expected: "El listado del feed debe ser ligero (solo URLs), no resolver cada video."
    steps_to_reproduce:
      - "tikdown-rs backfill run <user>"
      - "Observar 'Unexpected response from webpage request' en el listado"
    root_cause: "extract_info sin flat_playlist=True resuelve el feed completo, que TikTok bloquea."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "Anadir flat_playlist=True (listar URLs sin resolver)."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): flat-playlist en extract_profile"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221600"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "backfill"
    what_happened: "El fallo de UN video abortaba todo el feed y marcaba el backfill como failed (T5 violado - transitorio nunca definitivo)."
    what_expected: "Un video fallido (rate-limit/transitorio) no debe matar el backfill completo; debe continuar con el resto."
    steps_to_reproduce:
      - "Backfill de una cuenta con algun video bloqueado"
      - "Observar backfill.failed al primer ERROR"
    root_cause: "La excepcion de engine.download se propagaba al try catastrofe de run_backfill."
    files_changed: "src/tikdown_rs/services/backfill.py"
    approach: "try/except por video + continue (registrar y seguir, T5)."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): fallo de un video no aborta el feed (T5)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221700"
    date: "2026-08-28"
    severity: "medium"
    priority: "p2"
    scope: "backfill-engine"
    what_happened: "Sin ignoreerrors, una entrada bloqueada del feed abortaba TODO el listado del perfil."
    what_expected: "El listado debe tolerar entradas bloqueadas y continuar con las validas."
    steps_to_reproduce:
      - "extract_profile de una cuenta con entradas bloqueadas"
      - "Observar que el listado falla entero"
    root_cause: "yt-dlp propaga el error de una entrada como excepcion del listado completo."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "Anadir ignoreerrors=True (T5)."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): ignoreerrors en extract_profile"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221800"
    date: "2026-08-28"
    severity: "medium"
    priority: "p2"
    scope: "backfill-engine"
    what_happened: "yt-dlp con ignoreerrors dejaba entradas None en el feed, y el backfill crasheaba con AttributeError: 'NoneType' object has no attribute 'get'."
    what_expected: "El feed debe contener solo entradas validas."
    steps_to_reproduce:
      - "extract_profile con entradas bloqueadas"
      - "Observar AttributeError al iterar el feed"
    root_cause: "ignoreerrors deja huecos None que el caller no filtraba."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "Filtrar entradas e is not None en extract_profile."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): filtrar entradas None del feed"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T221900"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "download-engine"
    what_happened: "El engine nunca usaba las cookies descifradas (F-01/F-15): yt-dlp descargaba sin sesion y TikTok bloqueaba el feed (0 descargas)."
    what_expected: "El engine debe aplicar las cookies working (blob descifrado) a cada descarga/extraccion."
    steps_to_reproduce:
      - "Backfill con cookie valida importada"
      - "Observar 0 descargas + bloqueos de TikTok"
    root_cause: "YtDlpEngine no recibia ni aplicaba el blob de cookies; solo existia validate_cookie en el Protocol."
    files_changed: "src/tikdown_rs/core/download_engine.py, src/tikdown_rs/cli/backfill.py"
    approach: "Anadir cookies_blob al engine - cookiefile temporal; backfill descifra con Fernet y lo pasa."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): engine usa cookies descifradas (F-01/F-15)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222000"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "download-engine"
    what_happened: "El formato bestvideo+bestaudio (DASH) exigia separar video+audio - TikTok bloqueaba con Unable to extract universal data for rehydration. El formato simple best[height<=1080]/best descargaba OK."
    what_expected: "La descarga debe usar un formato que TikTok no bloquee (single, progresivo)."
    steps_to_reproduce:
      - "engine.download con DEFAULT_FORMAT DASH"
      - "Observar rehydration; repetir con formato single - OK"
    root_cause: "El merge DASH requiere resolver el video completo, que TikTok bloquea en este entorno."
    files_changed: "src/tikdown_rs/core/download_engine.py, tests/downloader/test_engine.py"
    approach: "Cambiar DEFAULT_FORMAT/RETRY_FORMAT a single-format y actualizar tests."
    risk_level: "high"
    new_tests: 2
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): formato single (evita resolver video+audio que TikTok bloquea)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222100"
    date: "2026-08-28"
    severity: "medium"
    priority: "p2"
    scope: "backfill-engine"
    what_happened: "impersonate_targets de curl-cffi (37 targets) reducian la extraccion del feed a 1 entrada (algunos targets fallan con TikTok y ignoreerrors los descarta)."
    what_expected: "El listado del feed debe devolver todas las entradas disponibles, con o sin impersonate."
    steps_to_reproduce:
      - "extract_profile con impersonate_targets - 1 entrada"
      - "extract_profile sin targets - 5+ entradas"
    root_cause: "Los targets de impersonacion interfieren con la extraccion del listado de TikTok."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "extract_profile sin impersonate (solo download lo usa, opt-in)."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): extract_profile sin impersonate"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222200"
    date: "2026-08-28"
    severity: "medium"
    priority: "p2"
    scope: "backfill-engine"
    what_happened: "El download con impersonate rompia la descarga (Unable to extract universal data for rehydration), mientras que sin impersonate descargaba OK."
    what_expected: "La descarga debe funcionar sin que el impersonate la rompa."
    steps_to_reproduce:
      - "engine.download con impersonate_targets - rehydration"
      - "engine.download sin targets - OK"
    root_cause: "El impersonate de curl-cffi no es compatible con la extraccion en este entorno."
    files_changed: "src/tikdown_rs/core/download_engine.py, src/tikdown_rs/cli/backfill.py"
    approach: "Impersonate opt-in via kwargs['use_impersonate']; backfill no lo pasa."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): impersonate opt-in"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222300"
    date: "2026-08-28"
    severity: "medium"
    priority: "p2"
    scope: "backfill"
    what_happened: "backfill_total se seteaba a len(feed_entries or []) = 0 cuando feed_entries=None, ANTES de listar - la contabilidad F-09 quedaba en 0 aunque hubiera entradas."
    what_expected: "backfill_total debe reflejar el numero real de entradas del feed al iniciar."
    steps_to_reproduce:
      - "backfill run (feed_entries=None)"
      - "Observar total=0 en backfill status"
    root_cause: "El total se calculaba sobre feed_entries crudo (None) antes de listar con _list_feed."
    files_changed: "src/tikdown_rs/services/backfill.py"
    approach: "Listar primero y setear backfill_total = len(entries) reales."
    risk_level: "medium"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): backfill_total con entradas reales tras listar (F-09)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222400"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "backfill-engine"
    what_happened: "El feed con cookies devolvia URLs de CDN con query strings gigantes; %(id)s generaba File name too long (Errno 36) y todas las descargas fallaban."
    what_expected: "Cada entrada del feed debe tener una URL de pagina (/video/<id>) que genere IDs cortos al descargar."
    steps_to_reproduce:
      - "Backfill de una cuenta con cookies"
      - "Observar 'File name too long' en las descargas"
    root_cause: "yt-dlp con cookies resolvia el feed a URLs de CDN con tokens largos; outtmpl='%(id)s' usaba el id gigante."
    files_changed: "src/tikdown_rs/core/download_engine.py"
    approach: "Normalizar cada entrada a URL de pagina https://www.tiktok.com/@user/video/<id>."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(backfill): normalizar URLs del feed a pagina"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-28T222500"
    date: "2026-08-28"
    severity: "high"
    priority: "p1"
    scope: "pacing"
    what_happened: "reserve_slot (pacing T62) usaba session.add + commit - IntegrityError con la fila singleton existente, rompiendo CADA descarga (por eso done=0 siempre)."
    what_expected: "El pacing debe reservar cooldown atomicamente sin fallar por la fila existente."
    steps_to_reproduce:
      - "Ejecutar backfill con pacing activo"
      - "Observar error SQL (INSERT pk duplicado) y 0 descargas"
    root_cause: "session.add no hace ON CONFLICT; con la fila id=1 existente lanzaba IntegrityError."
    files_changed: "src/tikdown_rs/core/pacing.py"
    approach: "Usar INSERT ... ON CONFLICT DO NOTHING nativo."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(pacing): INSERT ON CONFLICT nativo"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-29T003400"
    date: "2026-08-29"
    severity: "critical"
    priority: "p0"
    scope: "daemon-backfill"
    what_happened: "El job automatico del daemon backfill-collect fallaba con AttributeError: 'AsyncEngine' object has no attribute 'extract_profile' - TODO el backfill automatico del daemon estaba roto."
    what_expected: "El daemon debe recoger backfills queued/paused y ejecutarlos con el motor de descarga real (YtDlpEngine con cookies)."
    steps_to_reproduce:
      - "Re-encolar un backfill (--queue)"
      - "Esperar el ciclo de 60s del daemon"
      - "Observar task.failed en logs"
    root_cause: "_backfill_collect_job pasaba el AsyncEngine SQLAlchemy (para DB) como motor de descarga a collect_queued_backfills, que espera un YtDlpEngine."
    files_changed: "src/tikdown_rs/daemon/run.py"
    approach: "Construir YtDlpEngine(cookies_blob=blob) real con la cookie working descifrada en el job."
    risk_level: "high"
    new_tests: 0
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(daemon): backfill-collect usaba AsyncEngine como motor de descarga"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-29T003500"
    date: "2026-08-29"
    severity: "low"
    priority: "p3"
    scope: "backfill-cli"
    what_happened: "No existia forma de re-encolar un backfill terminado (completed/failed) para que el daemon lo reintentara - habia que resetear la DB a mano."
    what_expected: "Debe existir un comando que re-encole un backfill para reintento automatico."
    steps_to_reproduce:
      - "Backfill termina en completed con 0 descargas (rate-limit)"
      - "No hay comando para reintentar"
    root_cause: "Falta de feature: backfill run solo ejecutaba foreground, sin opcion de encolar."
    files_changed: "src/tikdown_rs/cli/backfill.py, src/tikdown_rs/services/backfill.py"
    approach: "Anadir flag --queue + requeue_backfill() (completed/failed - queued; backfilling - rechaza)."
    risk_level: "low"
    new_tests: 4
    type_check: "pass"
    lint: "pass"
    commit_type: "feat"
    release_type: "minor"
    commit_message: "feat(backfill): flag --queue para re-encolar backfills terminados"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-29T194800"
    date: "2026-08-29"
    severity: "critical"
    priority: "p0"
    scope: "daemon-cli"
    what_happened: "Bug #21 de la auditoria externa: `daemon stop` escribia stop_requested=True en daemon_state pero el daemon nunca leia esa columna - el flag era letra muerta y solo SIGTERM/SIGINT detenia el daemon."
    what_expected: "`daemon stop` debe detener el daemon con apagado limpio (drenaje de tareas, shutdown de bot y scheduler)."
    steps_to_reproduce:
      - "uv run tikdown-rs daemon run"
      - "En otra terminal: uv run tikdown-rs daemon stop"
      - "El daemon sigue corriendo indefinidamente (solo muere con SIGTERM)"
    root_cause: "El watcher del loop principal de _run() solo esperaba el stop_event de senales; ninguna ruta leia stop_requested de la DB (F-03 de la auditoria)."
    files_changed: "src/tikdown_rs/daemon/run.py, tests/daemon/test_runner.py"
    approach: "Watcher de _run() sondea stop_requested cada ~0.5s via asyncio.wait_for(stop_event.wait(), timeout=poll); al detectarlo consume el flag (reset a False, evita auto-apagado del proximo arranque) y activa el stop_event para apagado limpio."
    risk_level: "high"
    new_tests: 2
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(daemon): leer stop_requested en DB para que `daemon stop` funcione (bug #21)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-29T204100"
    date: "2026-08-29"
    severity: "medium"
    priority: "p2"
    scope: "telegram-bot"
    what_happened: "Bugs #22/#23 de la auditoria externa: (3.2) NotificationService.send_event era un mock sin envio real y el bus on_event no se propagaba desde el daemon; (3.3) el bot solo tenia /list - COMMANDS declaraba 12 comandos y dispatch() era codigo muerto que devolvia strings fijos."
    what_expected: "Decision tomada con el usuario: 3.2 -> marcar como no implementado (los ciclos emisores monitor/breaker/disco/red no se ejecutan hoy, T5.1); 3.3 -> implementar los 11 comandos restantes con paridad funcional CLI (§6.4)."
    steps_to_reproduce:
      - "Enviar /stats /disk /last /cookies /add /pause /resume /notify /monitor /backfill al bot"
      - "Respuesta: strings fijos '(via services/...)' sin acceso a datos"
    root_cause: "dispatch() nunca se conecto a services/*; NotificationService se quedo en esqueleto (T42) sin ciclos emisores que lo alimenten."
    files_changed: "src/tikdown_rs/daemon/telegram/handlers.py, src/tikdown_rs/daemon/telegram/bot.py, src/tikdown_rs/services/cookies.py, src/tikdown_rs/services/videos.py, src/tikdown_rs/core/notifications/telegram.py, src/tikdown_rs/daemon/run.py, README.md"
    approach: "3.3: dispatch() real sobre services/* en sesiones cortas (T32); /backfill solo re-encola (requeue_backfill); CommandHandlers registrados con auth+throttle compartidos; list_cookies()/list_recent() nuevas para paridad. 3.2: docstrings y README explicitos (noop) - reactivar exige epico propio (ciclos emisores + envio real + spool persistente + modo notifications)."
    risk_level: "medium"
    new_tests: 11
    type_check: "pass"
    lint: "pass"
    commit_type: "feat"
    release_type: "minor"
    commit_message: "feat(bot): implementar los 11 comandos restantes con paridad funcional CLI (auditoria 3.3-A)"
    follow_ups: "Epico futuro: notificaciones push reales (ciclos emisores + ExtBot + spool persistente)"
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-29T210400"
    date: "2026-08-29"
    severity: "medium"
    priority: "p2"
    scope: "cli-config"
    what_happened: "Bugs 3.4-3.7 de la auditoria externa: (3.4) .env.example desincronizado de Settings (7 variables muertas, 19 campos sin documentar, TELEGRAM_BOT_MODE=documentaba 'disabled' que no es valor valido); (3.5) videos integrity <username> filtraba account_id.is_not(None) ignorando el username (verificaba videos de CUALQUIER cuenta); (3.6) codigo muerto: _run() duplicado en download(), 2 metodos del Protocol DownloadEngine sin implementacion ni llamadores, asyncio.new_event_loop() muerto en run_daemon; (3.7) README decia 'epic e01 en curso' habiendo entregado e01-e15."
    what_expected: ".env.example sincronizado campo a campo y auditado por test; integrity filtra por username real via join; sin codigo muerto; README con estado real."
    steps_to_reproduce:
      - "3.5: videos integrity una_cuenta con videos de otra cuenta -> los verificaba igualmente"
      - "3.4: DOWNLOAD_TIMEOUT_SECONDS en .env no tiene ningun efecto (pydantic lo ignora)"
    root_cause: "3.5: where() incorrecto en vez de join con monitored_accounts; 3.4: drift entre .env.example documentado a mano y Settings; 3.6: restos de refactor nunca borrados."
    files_changed: ".env.example, tests/test_repo_hygiene.py, src/tikdown_rs/cli/videos.py, tests/integrity/test_videos_cli.py, src/tikdown_rs/core/download_engine.py, src/tikdown_rs/daemon/run.py, README.md"
    approach: "3.4: reescritura completa de .env.example + test de sincronia contra Settings.model_fields (excepcion FERNET_KEY via os.getenv); 3.5: _integrity_stmt() con join y test de regresion con 2 cuentas; 3.6: borrado de los 3 restos; 3.7: seccion Estado con 15 epicos y v0.3.2."
    risk_level: "medium"
    new_tests: 2
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(cli): videos integrity filtra por username real via join (auditoria 3.5)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-30T212900"
    date: "2026-08-30"
    severity: "low"
    priority: "p3"
    scope: "config"
    what_happened: "Hallazgo 4.1 de la auditoria externa: Settings usa extra=ignore, asi que una variable mal escrita (p.ej. HEARTBEAT_INTTERVAL_SECONDS) o muerta con prefijo TIKDOWN_/TELEGRAM_ se tragaba en silencio y el usuario creia tener una config activa que no aplicaba."
    what_expected: "Al construir Settings (arranque del daemon, CLI) se debe avisar con WARNING de toda variable con prefijo propio que no matchee un campo."
    steps_to_reproduce:
      - "TIKDOWN_HEARTBEAT_INTTERVAL_SECONDS=10 (typo) en .env"
      - "Arrancar el daemon: antes no habia ningun aviso"
    root_cause: "extra=ignore de pydantic-settings sin ningun chequeo complementario."
    files_changed: "src/tikdown_rs/core/config.py, tests/config/test_settings.py"
    approach: "warn_unknown_env_vars() compara os.environ con Settings.model_fields (prefijos TIKDOWN_/TELEGRAM_) y loggea config.unknown_env_var con la variable en extra; invocado desde Settings.__init__. 3 tests nuevos."
    risk_level: "low"
    new_tests: 3
    type_check: "pass"
    lint: "pass"
    commit_type: "feat"
    release_type: "minor"
    commit_message: "feat(config): warning al arrancar por variables de entorno desconocidas (auditoria 4.1)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-30T221200"
    date: "2026-08-30"
    severity: "critical"
    priority: "p0"
    scope: "daemon-monitor"
    what_happened: "Hallazgo 2.1 de la ronda 2 de auditoria: el daemon nunca ejecutaba el ciclo de monitor, el chequeo de disco ni el probe de red. run_monitor_cycle (la funcionalidad principal del producto: descubrir videos nuevos en cuentas mode=monitor), disk_job y NetworkMonitor existian bien testeados pero SIN NINGUN llamador; monitor start/stop escribian monitor_running en DB que nadie leia (el docstring del heartbeat prometia aplicarlo 'en caliente' y era falso)."
    what_expected: "monitor start/stop deben comandar el ciclo real del monitor en el daemon (aplicado en caliente por el heartbeat); disco y red con jobs periodicos propios, todo como tarea supervisada."
    steps_to_reproduce:
      - "monitor start (OK) con cuentas mode=monitor"
      - "Esperar: nunca se descubre video nuevo (run_monitor_cycle sin llamadores)"
      - "Disco llenandose: ninguna pausa automatica (disk_job sin llamadores)"
    root_cause: "Patron repetido de la ronda 1 (3.2/3.3): logica pura muy testeada en aislamiento + tests verdes que crean falsa sensacion de integracion; _register_jobs() solo registraba heartbeat y backfill-collect."
    files_changed: "src/tikdown_rs/daemon/monitor_job.py, src/tikdown_rs/daemon/run.py, src/tikdown_rs/core/config.py, .env.example, tests/daemon/test_monitor_job.py"
    approach: "MonitorJob: read_monitor_running() lee el flag (patron stop_requested 3.1) y arranca/drena el loop en caliente; ciclo = run_monitor_cycle + discover del daemon (YtDlpEngine + cookies working, registra Video, no toca backfill §10); heartbeat invoca read_monitor_running en cada tick. Jobs de disco (T65, 900s) y probe de red (§9) registrados en el scheduler. 5 tests de integracion nuevos."
    risk_level: "high"
    new_tests: 5
    type_check: "pass"
    lint: "pass"
    commit_type: "feat"
    release_type: "minor"
    commit_message: "feat(daemon): ejecutar el ciclo de monitor comandado por monitor_running (auditoria 2.1-r2)"
    follow_ups: "Cablear el bus on_event de los nuevos jobs a notificaciones (epico 3.2-B pendiente)"
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-30T221300"
    date: "2026-08-30"
    severity: "low"
    priority: "p3"
    scope: "config"
    what_happened: "Hallazgo 2.2 de la ronda 2: el warning de variables desconocidas (4.1) usaba _OWNED_PREFIXES=('TIKDOWN_','TELEGRAM_') — TIKDOWN_ no es prefijo de ningun campo real, asi que solo vigilaba TELEGRAM_ y el ejemplo del propio docstring (HEARTBEAT_INTTERVAL_SECONDS) NO se detectaba. Sin test dedicado."
    what_expected: "El warning debe cubrir los prefijos reales de la app (HEARTBEAT_, LOG_, DATA_, ...) y el caso del docstring debe estar testeado."
    steps_to_reproduce:
      - "HEARTBEAT_INTTERVAL_SECONDS=10 (typo) + arrancar el daemon"
      - "Antes: ningun warning (el prefijo no estaba vigilado)"
    root_cause: "Lista fija de prefijos que no correspondia a los nombres reales de Settings; test de 4.1 solo cubria TIKDOWN_/TELEGRAM_."
    files_changed: "src/tikdown_rs/core/config.py, tests/config/test_settings.py"
    approach: "_owned_prefixes() deriva los prefijos de Settings.model_fields (cobertura automatica ante campos nuevos) + namespaces de la app sin campos (TIKDOWN_ marca, FERNET_ secretos via os.getenv). Tests: caso del docstring + cobertura de prefijos de .env.example."
    risk_level: "low"
    new_tests: 2
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(config): warning de env cubre los prefijos reales derivados de Settings (auditoria 2.2-r2)"
    follow_ups: null
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-30T230200"
    date: "2026-08-30"
    severity: "critical"
    priority: "p0"
    scope: "daemon-monitor"
    what_happened: "Hallazgos 2.1-bis y 2.1-ter de la ronda 3: (bis) daemon_discover insertaba Video.status='new' fuera del CHECK ck_videos_status (downloaded|failed|cancelled|skipped) — el commit fallaba silenciosamente (run_loop traga el error con WARNING) justo cuando el monitor encontraba contenido nuevo; el test de ronda 2 no lo atrapo porque usaba un discover fake que no pasaba status ni ejercitaba daemon_discover real (que ademas construia YtDlpEngine real e ignoraba inyecciones - hizo una llamada de red real en un test). (ter) aunque el CHECK se arreglara, nadie consumia los pending: el monitor descubria pero no descargaba."
    what_expected: "daemon_discover persiste 'pending' sin violar constraint (testado con la funcion real contra DB con CHECK activo); el monitor descarga automaticamente lo descubierto con el mismo motor y pacing T62 que el backfill."
    steps_to_reproduce:
      - "monitor start con cuenta mode=monitor y cookie working"
      - "La cuenta sube un video nuevo → IntegrityError en el commit → solo un WARNING en el log"
      - "Ningun video aparece (indistinguible de 'no ha subido nada')"
    root_cause: "Desconocimiento del CHECK del schema al escribir el discover (mi bug, introducido en el fix 2.1 de la ronda 2); test de integracion con discover fake en vez de la funcion real."
    files_changed: "alembic/versions/a1b2c3d4e5f6_video_pending_status.py, src/tikdown_rs/models/models.py, src/tikdown_rs/daemon/monitor_job.py, src/tikdown_rs/daemon/run.py, tests/daemon/test_monitor_job.py, tests/daemon/test_pending_migration.py, README.md"
    approach: "Migracion batch_alter_table del CHECK + 'pending'; daemon_discover con status='pending' y URL normalizada; MonitorJob.engine/cookies_blob inyectables; download_pendings() con reserve_slot T62, downloaded/skipped/failed, un fallo no aborta el lote (T5); _run_one_cycle() = discover + download_pendings. Tests: migracion sobre el camino real (old schema → rechazo → migrar → OK), discover real con extract_profile mockeado y CHECK activo, ciclo completo descubre-descubre-descarga con engine fake."
    risk_level: "high"
    new_tests: 3
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "fix(models): CHECK de Video.status admite 'pending' via migracion Alembic (auditoria 2.1-bis)"
    follow_ups: "Ciclo end-to-end real contra TikTok cuando lo permita el rate-limit (Fase 4 ronda 1, sigue pendiente)"
    file: null
    status: "fixed"
  - bug_id: "BUG-2026-08-30T232900"
    date: "2026-08-30"
    severity: "low"
    priority: "p3"
    scope: "daemon-monitor"
    what_happened: "Hallazgos 3.1/3.2/3.3 de la ronda 4 (menores, no bloqueantes): (3.1) la migracion a1b2c3d4e5f6_pending_status tenia revision id hecho a mano (hex secuencial + slug pegado, no generado por alembic revision); (3.2) download_pendings detectaba slideshow con heuristica propia debil bool(info.get('formats')) en vez de verify_video()+classify_integrity; (3.3) marcaba todos los fallos 'transient' sin classify_failure."
    what_expected: "Trazabilidad alembic real (id generado por la herramienta); verificacion post-descarga con ffprobe real; clasificacion de fallos con el clasificador del proyecto."
    steps_to_reproduce:
      - "3.2: slideshow real registrado como downloaded (formats no vacia)"
      - "3.3: video eliminado de TikTok quedaba 'transient' y reintentaba forever"
    root_cause: "Reinvencion de utilidades existentes al escribir download_pendings de forma aislada; id de migracion inventado por no ejecutar alembic revision."
    files_changed: "alembic/versions/c09bde76270e_video_pending_status.py, src/tikdown_rs/daemon/monitor_job.py, tests/daemon/test_pending_migration.py"
    approach: "Migracion regenerada con alembic revision (c09bde76270e) y test a 'upgrade head'; download_pendings usa verify_video (ffprobe) + classify_integrity con mapeo 'integrity'->failed (CHECK no admite integrity en status) + classify_failure para la categoria; test con FakeEngine que escribe archivo y ffprobe mockeado."
    risk_level: "low"
    new_tests: 1
    type_check: "pass"
    lint: "pass"
    commit_type: "fix"
    release_type: "patch"
    commit_message: "refactor(monitor): verify_video + classify_failure en download_pendings (auditoria 3.2/3.3)"
    follow_ups: "run_backfill (services/backfill.py) hereda la misma carencia de classify_failure en su except - NO se toca porque backfill no crea filas Video (error_category es de Video); revisar si backfill debe registrar videos."
    file: null
    status: "fixed"
